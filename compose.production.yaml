# Общий anchor гарантирует один immutable image и единое окружение для всех Node-процессов.
x-app: &app
  image: ${IMAGE:-ghcr.io/komaroffsergei/fullstack-test-shop:latest}
  restart: unless-stopped
  env_file: .env.production
  depends_on:
    postgres:
      condition: service_healthy

services:
  # PostgreSQL не публикуется наружу и доступен только внутри Compose network.
  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: shop
      POSTGRES_USER: shop
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U shop -d shop']
      interval: 3s
      timeout: 3s
      retries: 30
    volumes:
      - postgres-data:/var/lib/postgresql/data

  # Единственная внешняя точка входа слушает только loopback VDS для Nginx upstream.
  app:
    <<: *app
    command: ['node', 'apps/api/dist/main.js']
    ports:
      - '127.0.0.1:4400:4000'

  # Worker не открывает HTTP-порт и читает очередь прямо из PostgreSQL.
  worker:
    <<: *app
    command: ['node', 'apps/worker/dist/main.js']

  # Две роли используют один образ, различаясь только PROVIDER_ID и внутренним портом.
  provider-a:
    <<: *app
    command: ['node', 'apps/mock-provider/dist/main.js']
    environment:
      PROVIDER_ID: A
      PORT: 4101

  provider-b:
    <<: *app
    command: ['node', 'apps/mock-provider/dist/main.js']
    environment:
      PROVIDER_ID: B
      PORT: 4102

volumes:
  postgres-data:
